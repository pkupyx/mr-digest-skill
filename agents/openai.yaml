interface:
  display_name: "MR Digest Skill"
  short_description: "Generate a single Tree-sitter MR diff report"
  default_prompt: "Use $mr-digest-skill to analyze a merge request and produce one diff.md report."

policy:
  allow_implicit_invocation: false
